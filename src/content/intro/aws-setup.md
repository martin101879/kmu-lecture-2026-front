# AWS 인프라 셋업

> **이 단계를 가장 먼저 진행합니다.** 인프라 생성에 약 10분이 소요되므로, 먼저 실행해놓고 다음 강의를 진행합니다.

## CloudFormation이란?

AWS 리소스(서버, 데이터베이스, 스토리지 등)를 **코드로 정의하고 한 번에 생성**해주는 서비스입니다.
콘솔에서 하나씩 클릭하며 만드는 대신, 스크립트 하나로 전체 인프라를 자동 구성합니다.

## 실행 방법

### 1. AWS 콘솔 로그인

[https://aws.amazon.com](https://aws.amazon.com) 에 로그인하고, 우측 상단에서 리전이 **서울 (ap-northeast-2)** 인지 확인합니다.

### 2. CloudFormation 스택 생성

1. AWS 콘솔 상단 검색창에 **CloudFormation** 입력 → 클릭
2. **스택 생성** 버튼 클릭
3. "템플릿 지정"에서 **"Amazon S3 URL"** 선택 후 아래 URL 입력:

```
(강의 당일 제공 예정)
```

4. **스택 이름** 입력: `capstone-{본인이름}` (예: `capstone-홍길동`)
5. 나머지는 기본값 유지 → **다음** → **다음** → **제출**

### 3. 생성 확인

- 상태가 `CREATE_IN_PROGRESS` → `CREATE_COMPLETE`로 변경되면 완료
- 약 **10분** 정도 소요됩니다

## 생성되는 리소스

| 리소스 | 용도 |
|--------|------|
| S3 Bucket | React 프론트엔드 정적 파일 호스팅 |
| CloudFront | CDN + HTTPS 제공 |
| EC2 | Spring Boot 백엔드 서버 |
| RDS (MySQL) | 데이터베이스 |
| Security Group | EC2/RDS 네트워크 접근 제어 |

> 생성된 리소스의 상세 설명은 코드 설명이 끝난 후 **AWS 인프라 & 배포** 섹션에서 다룹니다.

## 강의 종료 후 정리

강의가 끝나면 **반드시 스택을 삭제**해주세요. 삭제하지 않으면 비용이 계속 발생합니다.

1. CloudFormation → 해당 스택 선택
2. **삭제** 버튼 클릭
3. 모든 리소스가 자동으로 제거됩니다
